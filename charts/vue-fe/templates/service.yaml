apiVersion: v1
kind: Service
metadata:
  name: vue-fe-service
  namespace: app
  labels:
    app: vue-fe
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: vue-fe